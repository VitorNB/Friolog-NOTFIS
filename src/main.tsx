import React from 'react';
import ReactDOM from 'react-dom/client';
import NotfisReader from './notfis_reader'; // Importa seu componente

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NotfisReader />
  </React.StrictMode>,
);